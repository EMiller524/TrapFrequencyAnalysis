# Pull in the repo’s core deps
-r ../requirements.txt
# App-specific deps
streamlit>=1.37,<2
pandas>=2.0,<3
numpy>=2.0,<3
altair>=5,<6
matplotlib>=3.8,<4



# # Pull in the repo’s core deps
# -r ../requirements.txt

# # App-specific deps
# streamlit>=1.37,<2
# pandas>=2.0,<3
# If you don’t want a root requirements.txt, just put everything into iontrap_applet/requirements-app.txt.

